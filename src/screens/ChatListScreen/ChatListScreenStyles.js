import { StyleSheet } from "react-native";

const ChatListScreenStyles = StyleSheet.create({
    wrapper: {
        flex: 1,
        backgroundColor: "red",
    },
});

export default ChatListScreenStyles;
